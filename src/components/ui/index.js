// Barrel exports for UI components.
// Intentionally missing the export for './Text' (students must add it).
// Barrel file for UI components
// This allows you to import all UI components from one place
export { default as Button } from './Button';
export { default as TextInput } from './TextInput';
export { default as Text } from './Text'; //  Fixed: now Text is properly exported
